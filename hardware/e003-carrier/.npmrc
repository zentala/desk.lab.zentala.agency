# tscircuit currently publishes peer ranges that overlap across its renderer
# packages. Keep npm ci reproducible without requiring each contributor to know
# the workaround flag.
legacy-peer-deps=true
