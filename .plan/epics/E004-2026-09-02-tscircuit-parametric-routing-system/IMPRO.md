# E004 improvements

- [ ] Evaluate whether route solving should remain inside tscircuit or use a
      KiCad-compatible export/solver if the pinned autorouter cannot produce a
      deterministic, reviewable two-layer result.
- [ ] Add a future 3D/mechanical clearance adapter after the first physical
      prototype supplies measured component heights.
- [ ] Add a shared component package only after a second real hardware project
      consumes the primitives; until then keep the library local to E003.
- [ ] Add manufacturing-house rule profiles after the target fabrication house
      and assembly process are selected.
