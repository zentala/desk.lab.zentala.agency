---
id: E001-T05
epic: E001
status: backlog
created: 2026-07-28
completed:
branch: feat/E001-T05-domain
---
# E001-T05: Resolve canonical domain; delete root `CNAME`

**Wave 1** · canonical domain **decided: `desk.labs.zentala.agency`** (plural). `lab` (singular) is the bug.

## Goal
Make the plural domain canonical everywhere and remove the GitHub-Pages `CNAME` mechanism.

## Acceptance
- Canonical string is **`desk.labs.zentala.agency`** — owner resolved (2026-07-28): `labs`, not `lab`.
- Every `desk.lab.zentala.agency` (singular) reference fixed to `labs` — CI, docs, and the `master`
  `CNAME`. (`dev` tree already uses `labs`; the singular lives only on `master`'s CNAME + old notes.)
- Root `CNAME` deleted (GitHub Pages mechanism; Cloudflare Pages ignores it).
