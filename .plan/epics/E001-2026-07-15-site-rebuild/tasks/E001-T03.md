---
id: E001-T03
epic: E001
status: backlog
created: 2026-07-28
completed:
branch: feat/E001-T03-rescue-legacy-notes
---
# E001-T03: Rescue `legacy/notes/*.md`, then remove `legacy/`

**Wave 1** · sequential · depends on T02.

## Goal
Preserve the article source notes, then delete the history-disconnected `legacy/` duplicate.

## Acceptance
- `legacy/notes/*.md` (article-draft, voice-stream, any others) moved to a kept location
  (e.g. `.plan/epics/E001-.../reports/sources/` or `research/notes/`) via `git mv`.
- `git rm -r legacy/` after the merge lands (it is a hand-copied duplicate of master's content).
- No article source lost; links updated.

## Notes
Confirm nothing in `legacy/` is referenced by the app before removal.
