---
id: E001-T02
epic: E001
status: backlog
created: 2026-07-28
completed:
branch: consolidate/site-rebuild
---
# E001-T02: Execute the merge; resolve conflicts by hand

**Wave 1** · sequential · depends on T01.

## Goal
Merge `master` into `dev` on a `consolidate/site-rebuild` branch, resolving every conflict manually.

## Acceptance
- Merge performed on `consolidate/site-rebuild` (branched from `dev`).
- Each conflict from T01 resolved **by hand** — never `--ours`/`--theirs` wholesale.
- `git log --follow` still resolves the 2023 photo commits (history intact).
- Build stays green (`cd astro && npm run build`).

## Notes
This is the owner's hard-learned lesson: wholesale `--ours` loses incoming work. Open both sides, merge by hand.
