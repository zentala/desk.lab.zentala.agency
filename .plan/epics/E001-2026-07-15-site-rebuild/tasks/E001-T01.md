---
id: E001-T01
epic: E001
status: backlog
created: 2026-07-28
completed:
branch: feat/E001-T01-merge-rehearsal
---
# E001-T01: Rehearse the `master` merge in a throwaway worktree

**Wave 1** · sequential. See [../ORCHESTRATOR.md](../ORCHESTRATOR.md).

## Goal
De-risk the branch consolidation by rehearsing `git merge master` into `dev` in a disposable
worktree, and documenting the exact conflict set before touching the real branch.

## Acceptance
- Throwaway worktree created off `dev`; `git merge master` run there (not committed to a real branch).
- Conflict set documented in [../JOURNAL.md](../JOURNAL.md) — expected: `CNAME`, `README.md`,
  `CLAUDE.md`, `.editorconfig`, `.gitignore`.
- Confirmed: `master`/`dev` share ancestor `e1a248e` (no `--allow-unrelated-histories` needed).
- Worktree removed cleanly afterwards.

## Notes
Assets (`index.html`, `style.css`, `images/`, `enclosure/`) must show as clean adds, not conflicts.
