---
id: E001-T12
epic: E001
status: backlog
created: 2026-07-28
completed:
branch: feat/E001-T12-cutover
---
# E001-T12: Cutover without going dark; freeze `master`

**Wave 3** · **last website task in the program** · depends on T05/T09/T10 **and E002 content live**.

## Goal
Point the canonical domain at the new app without downtime, then freeze the old site.

## Acceptance (ordered — do not reorder)
1. Verify the new site on its `*.pages.dev` URL.
2. Add the canonical domain (T05) as a Cloudflare Pages custom domain.
3. Flip DNS (instant, instantly revertible).
4. Wait 24–48h.
5. **Disable GitHub Pages in repo settings** — DNS alone leaves `*.github.io` serving forever.
6. Tag `archive/master-static-site-final`; freeze `master` (do not delete).

## Notes
`desk.lab.zentala.agency` is live on GH Pages *now* — the sequence must never leave it dark.
