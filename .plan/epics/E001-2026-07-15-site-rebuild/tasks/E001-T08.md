---
id: E001-T08
epic: E001
status: backlog
created: 2026-07-28
completed:
branch: feat/E001-T08-move-funnel
---
# E001-T08: Move the funnel to `/lp`; invert `noindex`

**Wave 2** · **paired with E002's report homepage** — do not leave `/` empty.

## Goal
Relocate the pre-order funnel under `/lp` and flip the indexing policy for going public.

## Acceptance
- `src/pages/index.astro` (funnel) → `src/pages/lp/index.astro`, components unchanged.
- `noindex` kept **only** on `/lp`; removed everywhere else.
- `robots.txt` restored to `Allow: /` + sitemap.

## Tests
Release-blocking gate: `noindex` present on `/lp`, absent on every public page.
