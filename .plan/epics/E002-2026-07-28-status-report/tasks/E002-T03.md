---
id: E002-T03
epic: E002
status: backlog
created: 2026-07-28
completed:
branch: feat/E002-T03-updates-collection
---
# E002-T03: Add the `updates` content collection

**Wave 1** · depends on T01.

## Goal
A typed, versioned collection for status-report entries so bad frontmatter fails the build.

## Acceptance
- `updates` collection in `astro/src/content.config.ts`: `title`, `description`, `version`,
  `statusDate` (`z.coerce.date()`), `status` (`current|superseded`), `supersedes` (optional), `tags`.
- A stub entry validates; a deliberately-bad `statusDate` fails `astro build`.
