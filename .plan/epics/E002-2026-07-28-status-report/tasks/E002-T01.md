---
id: E002-T01
epic: E002
status: backlog
created: 2026-07-28
completed:
branch: feat/E002-T01-mdx-pipeline
---
# E002-T01: Wire the MDX + typography pipeline

**Wave 1** · foundation · depends on E001-T02 (merge) landed.

## Goal
Make MDX and prose styling available so the article and report pages can be authored.

## Acceptance
- `@astrojs/mdx` installed and added to `astro.config`.
- `@tailwindcss/typography` installed; a `prose` variant renders in a throwaway `.mdx` page.
- `astro build` exits 0 with an MDX page present.

## Notes
Current content glob is `**/*.md` only; neither package is installed on `dev`. **npm**, not pnpm.
