---
id: E002-T07
epic: E002
status: backlog
created: 2026-07-28
completed:
branch: feat/E002-T07-report-home
---
# E002-T07: Report homepage at `/`

**Wave 2** · depends on T03, T05, T06 · **pairs with E001-T08** (funnel → `/lp`).

## Goal
Make `/` render the current status report instead of the funnel.

## Acceptance
- `/` shows the status header (T06) + the current `updates` entry (T08) + decision records (T05).
- Coordinated with E001-T08 so exactly one thing owns `/` (no route conflict).
- `astro build` exits 0; `/` has no pricing/CTA/scarcity per `design.md`.
