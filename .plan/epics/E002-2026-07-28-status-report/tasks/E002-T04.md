---
id: E002-T04
epic: E002
status: backlog
created: 2026-07-28
completed:
branch: feat/E002-T04-retheme-primitives
---
# E002-T04: Re-theme the orphaned `ui/` primitives

**Wave 1** · depends on T02.

## Goal
Revive the existing structural primitives against real tokens instead of rewriting them.

## Acceptance
- `Section`, `SectionHeader`, `InfoCard`, `StatCard`, `FigureSwap` render with no missing-token
  fallbacks (they currently reference non-existent `text-ink`, `.card`, `shadow-soft`, `bg-fog`).
- Each maps to the role assigned in `design.md` (T02).

## Notes
Revive, don't rewrite — structure is fine, only theming is broken.
